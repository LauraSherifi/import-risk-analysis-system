This folder contains the team notebook workspace for algorithm experiments,
visualizations, and presentation-ready analysis.

Notebook files are intentionally created empty so each team member can build
their assigned work in cells while the Python scripts in `ml/src` remain the
main reusable implementation for the project.
